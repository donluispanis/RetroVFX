name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: build_debug
      run: ./build_all_linux.sh
    - name: build_release
      run: ./build_all_linux_release.sh
